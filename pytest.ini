[pytest]
addopts = -m "not external"
markers =
    unit: fast local unit tests without network dependencies
    integration: tests that require local backend/services
    external: opt-in tests that hit third-party APIs (Binance/Alpaca)
